export default function PayrollPage() {
  return (
    <div>
      <p style={{ fontFamily: "var(--font-mono)", fontSize: "11px", color: "var(--cyan)", letterSpacing: "0.1em", textTransform: "uppercase", marginBottom: "8px" }}>Payroll</p>
      <h1 style={{ fontFamily: "var(--font-display)", fontWeight: 800, fontSize: "32px", letterSpacing: "-0.03em", color: "var(--text)", marginBottom: "16px" }}>Payroll Runs</h1>
      <p style={{ fontFamily: "var(--font-display)", fontSize: "14px", color: "var(--text-muted)" }}>Coming in Phase 3 — private batch payments via Umbra mixer.</p>
    </div>
  );
}